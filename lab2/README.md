## To install dependicies and build project run:

```./build.sh```

## To clean build driectory:

```rm -rf build```

## To run tests

```
cd build
./bst_test
```

## To run gui:

```./run_gui.sh``

## TODO: DEV

* Test gui (iterator movement)
* Add profiler program to calclate big(O) stuff